# Web framework
fastapi
uvicorn[standard]

# Database
sqlalchemy

# Validation / serialisation
pydantic[email]

# HTTP & feed parsing (stdlib xml used for RSS; no feedparser needed)
aiohttp
requests
certifi

# Sentiment analysis
textblob

# HTML parsing (used by legacy finance routes)
beautifulsoup4

# Config
python-dotenv

# Templating (admin UI)
jinja2

# Testing
pytest
httpx
pytest-asyncio
pytest-mock
pytest-cov
aioresponses
responses
